[project]
name = "aoe-retarget-lab"
version = "0.1.0"
description = "Open-AoE object reconstruction and robot retargeting matrix runner"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
